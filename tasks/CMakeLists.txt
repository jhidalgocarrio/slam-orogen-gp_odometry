# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(gp_odometryTaskLib)
ADD_LIBRARY(${GP_ODOMETRY_TASKLIB_NAME} SHARED 
    ${GP_ODOMETRY_TASKLIB_SOURCES})
add_dependencies(${GP_ODOMETRY_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${GP_ODOMETRY_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${GP_ODOMETRY_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${GP_ODOMETRY_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${GP_ODOMETRY_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${GP_ODOMETRY_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${GP_ODOMETRY_TASKLIB_HEADERS}
    DESTINATION include/orocos/gp_odometry)

